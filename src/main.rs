use std::path::Path;

// #[inject_temporary_folder]
// fn my_func(path: &Path) {
//     println!("Ici");
//     println!("{:?}", path);
// }

fn main() {
    // let path = Path::new("/tmp/42");
    // my_func(&path);
}
